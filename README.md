# Social Hub — Next.js + Tailwind + TS (Starter)

**Objectif :** Landing + Auth placeholders + Dashboard mocké + utilitaire UUID.

## Démarrage
```bash
pnpm i   # ou npm i / yarn
pnpm dev # http://localhost:3000
```

## Étapes suivantes
- Intégrer Auth.js (NextAuth) + Prisma (UUID côté serveur)
- Page d'onboarding pour connecter YouTube / TikTok / WordPress
- API routes + CRON pour sync stats
- Remplacer les placeholders
