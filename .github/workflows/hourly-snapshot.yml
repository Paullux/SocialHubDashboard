name: Hourly Snapshot

on:
  schedule:
    # 12 et 42 min après l'heure : évite le créneau saturé "pile à l'heure" et
    # se donne un 2e passage si le 1er est retardé/sauté (l'upsert rend ça idempotent).
    - cron: "12 * * * *"
    - cron: "42 * * * *"
  workflow_dispatch: {}    # permet de lancer manuellement aussi

jobs:
  call-snapshot:
    runs-on: ubuntu-latest

    steps:
      - name: Hit snapshot endpoint
        env:
          BASE_URL: ${{ secrets.PROD_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          echo "Calling $BASE_URL/api/cron/snapshot"
          curl -sS --fail-with-body "${BASE_URL}/api/cron/snapshot?key=${CRON_SECRET}" \
            --max-time 90 \
            -H "User-Agent: github-actions-cron"
